# pytest.ini — Kerno test runner configuration
# Placed at the repository root.
#
# Ensures pytest discovers ALL test directories:
#   src/**/tests/        unit tests, travel with their layer
#   tests/integration/   tests requiring a live database or external service
#
# See FILE_STRUCTURE.md for the full test placement rule.

[pytest]
testpaths =
    src
    tests

python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts =
    --tb=short
    --strict-markers
    -q

markers =
    unit: Fast unit tests with no external dependencies
    integration: Tests requiring a live PostgreSQL database
    slow: Tests that take more than 2 seconds

minversion = 7.0
